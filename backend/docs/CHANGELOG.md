# Changelog

All notable changes to this project are documented here.

## [0.1.0] - Unreleased

### Added
- Initial project scaffold following the Project Scaffolding Standard.
- `backend/` (FastAPI + SQLAlchemy + SQLite) and `frontend/` (React + Vite + TS).
- Stub models (Language, Book, Text, Term), routers, and SPA pages
  (Library, Reader, Account).
- Thin run wrappers (`run.bat`, `run.sh`).
